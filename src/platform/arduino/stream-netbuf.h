#pragma once

// arduino stream-netbuf adapter
#include <Arduino.h>

namespace moducom { namespace mem {

// Utilize still-unfinished MemoryNetBuf (not necessarily dynamic one)
// to do this we'll possibly have to split out write-netbufs from read-netbufs
class StreamNetbuf
{

};

}}